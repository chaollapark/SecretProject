import { Keypair } from '@_koii/web3.js';
import { FileUploadResponse, InitOptions } from './Interfaces';
export declare class KoiiStorageClient {
    private static instance;
    private storageTaskId;
    private k2URL;
    private taskState;
    private trustedNodes;
    private debug;
    private constructor();
    private watchTaskState;
    uploadFile(file: string | File | File[], stakingWallet: Keypair): Promise<FileUploadResponse>;
    getFile(cid: string, filename: string): Promise<Blob>;
    signingFileHash(fileHash: string, stakingWallet: Keypair): Promise<string>;
    private hashFileData;
    private bs58Encode;
    private getTaskState;
    private getNodesRunningTask;
    saveBlobAsFile(blob: Blob, filename: string): Promise<void>;
    private shuffleArray;
    private sleep;
    private fetchWithTimeout;
    static getInstance(options: InitOptions): KoiiStorageClient;
}
//# sourceMappingURL=index.d.ts.map